import React from 'react';

const Poster = ({ color }) => {
    return (
        <div className='w-[175px] h-60 rounded-[35px]' style={{ backgroundColor: color }}>
            
        </div>
    );
};

export default Poster;